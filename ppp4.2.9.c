#include <stdio.h>

int main(){
    int e, p, m;
    printf("Indique o montante do empréstimo: ");
    scanf("%d", &e);
    printf("Indique o montante do pagamento mesal: ");
    scanf("%d", &p);
    printf("Indique o número de meses que quer calcular: ");
    scanf("%d", &m);

}